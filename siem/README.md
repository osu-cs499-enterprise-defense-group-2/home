This is the same os version as the ubuntuserver.

The benchmark scripts are located in ubuntuserver/scripts.

Any additional tasks needed to protect this vm (siem) should be added within this directory.
